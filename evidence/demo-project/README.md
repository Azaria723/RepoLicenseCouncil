# Synthetic license fixture

This directory represents a fictional repository component used only for reproducible contract testing. GitHub is authoritative for the bytes committed here, but the fixture makes no claim about any external package, organization or legal event.

The live demonstration must compare two immutable commits of this exact `LICENSE` path. The second commit replaces the baseline with an explicitly identified synthetic network-copyleft example.
